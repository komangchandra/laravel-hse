@extends('layouts.admin')
@section('title', 'Audit Operasional')
@section('content')
<div class="mb-4"><h4 class="fw-bold mb-1">Audit Operasional</h4><p class="text-muted mb-0">Jejak append-only untuk perubahan workflow, keputusan, penerbitan, dan cetak kartu.</p></div>
<div class="card border-0 shadow-sm"><div class="card-header bg-white"><form class="row g-2"><div class="col-md-4"><input name="action" value="{{ request('action') }}" class="form-control" placeholder="Cari aksi"></div>@if(auth()->user()->isDeveloper())<div class="col-md-3"><select name="owner_id" class="form-select"><option value="">Semua owner</option>@foreach($owners as $owner)<option value="{{ $owner->id }}" @selected((int)request('owner_id') === $owner->id)>{{ $owner->legal_name }}</option>@endforeach</select></div>@endif<div class="col-md-2"><input type="date" name="date_from" value="{{ request('date_from') }}" class="form-control"></div><div class="col-md-2"><input type="date" name="date_to" value="{{ request('date_to') }}" class="form-control"></div><div class="col-md-1"><button class="btn btn-primary w-100">Filter</button></div></form></div>
<div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-3">Waktu</th><th>Aktor / organisasi aktor</th><th>Aksi</th><th>Subject</th><th>Perubahan</th><th>IP</th></tr></thead><tbody>@forelse($logs as $log)<tr><td class="ps-3 small">{{ $log->occurred_at->format('d-m-Y H:i:s') }}</td><td>{{ $log->actor_name ?? 'Sistem' }}<div class="small text-muted">Actor org #{{ $log->actor_organization_id ?? '-' }} / scope {{ $log->organization?->short_name ?? '-' }}</div></td><td><code>{{ $log->action }}</code></td><td class="small">{{ class_basename($log->subject_type) }} #{{ $log->subject_id }}</td><td class="small">{{ $log->from_status ?? '-' }} → {{ $log->to_status ?? '-' }}</td><td class="small">{{ $log->ip_address ?? '-' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted p-5">Belum ada audit.</td></tr>@endforelse</tbody></table></div>@if($logs->hasPages())<div class="card-footer bg-white">{{ $logs->links() }}</div>@endif</div>
@endsection
